#include<iostream>
#include<vector>
using namespace std;
int main(){
    vector<int>v;
    v.push_back(8);
    v.push_back(9);
    v.push_back(4);
    v.push_back(2);
    for(int i=0;i<=3;i++)
    cout<<v[i]<<endl;
}